هنا تظهر ملفات الموديل بعد تشغيل:
  PP/ai/export_for_flutter.py

المتوقع:
  exercise_classifier.tflite
  scaler_params.json
  classes.json

لو الملفات مش موجودة، شاشة التمارين في التطبيق هتعرض إن الموديل لسه ما اتصدّرش.
